class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        l=0
        count=1
        for i in range(len(nums)):
            if nums[l]!=nums[i]:
                nums[l+1], nums[i]=nums[i], nums[l+1]
                l+=1
                count+=1
        return count
